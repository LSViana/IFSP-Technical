package ifsp.lp2.testb1.exercise3;

public class CollectionsMethods {
	
	/*
	 A classe Collections contém apenas métodos estáticos para operar sobre coleções, tendo como seus principais métodos:
	 copy(List, List): copia uma lista (primeiro parâmetro) para a outra (segundo parâmetro)
	 reverse(List): inverte a ordem dos itens na lista
	 sort(List): ordena os itens da lista utilizando os seus métodos compareTo (método mergeSort dentro da classe java.util.Arrays)
	 addAll(Collection, Object[]): adiciona um vetor de itens na coleção fornecida como parâmetro
	 */

}
